branch: master
allowedFields: ["name", "rsvp", "dish"]
commitMessage: "Add new RSVP by {{fields.name}}"
filename: "rsvps/{{slug}}.json"
slug:
  encoding: ascii
  clean: true
transforms:
  rsvp:
    enabled: false
